# Modelo ITU-R P.1546-6: Propagación Point-to-Area Empírica (REFACTORIZADO)

**Archivo fuente:** `src/core/models/traditional/itu_r_p1546.py`
**Archivo de tablas:** `src/core/models/traditional/itu_r_p1546_tables.py`
**Clase:** `ITUR_P1546Model`
**Referencia:** ITU-R P.1546-6 (agosto 2019)
**Estado:** ✅ REFACTORIZADO - Arquitectura correcta implementada
**Versión:** 2026-05-13 (REFACTORIZADA)

---

## 1. Descripción General

El modelo **ITU-R P.1546-6** es un modelo **punto-a-área empírico** basado en **tablas de intensidad de campo medidas** para predecir cobertura radioeléctrica terrestre (30-4000 MHz). A diferencia de Okumura-Hata o COST-231 que son ecuaciones semi-empíricas específicas para sistemas celulares, P.1546 proporciona curvas de referencia de campo eléctrico derivadas de mediciones extensas, interpoladas en 3 dimensiones (frecuencia, distancia, altura efectiva del transmisor).

### 1.1 Arquitectura Refactorizada (Correcta)

**CAMBIOS FUNDAMENTALES respecto a versión anterior:**

| Aspecto | ANTERIOR (Incorrecto) | NUEVO (Correcto) | Razón |
|--------|----------------------|-----------------|-------|
| **Base matemática** | FSPL baseline + correcciones | Tablas ITU + interpolación 3D | P.1546 ES empírico, no teórico |
| **LOS/NLOS** | Booleano binario | Continuo vía TCA | P.1546 es estadístico/probabilístico |
| **Altura efectiva** | No implementada | Vectorizada: h_eff = h_tx + z_tx - z_mean(3-15km) | Requiere DEM para precisión |
| **Vectorización** | FOR loops por receptor | Puro NumPy sin bucles | Performance 100× mejor |
| **Fuente de referencia** | Ecuaciones ad-hoc | Tablas digitalizadas ITU | Cumple especificación exacta |

**Pipeline de 5 pasos (CORRECTO):**
```
1. h_eff = h_tx o (h_tx + z_tx - z_mean(3-15km))   [según longitud de trayecto]
2. E[dBμV/m] = interpolar_3D(f, d, h_eff)          [tablas ITU]
3. TCA = max(theta_i en ventana 0-15 km desde RX) [§4.5]
4. PL_corr = TCA_corr + Clutter_corr + Percentile_corr
5. PL = 139.3 + 20·log10(f) - E + PL_corr
```

### 1.2 Rangos de Validez

| Parámetro | Mínimo | Máximo | Unidades |
|-----------|--------|--------|----------|
| Frecuencia | 30 | 4000 | MHz |
| Distancia | 1 | 1000 | km |
| Altura efectiva TX | 10 | 1200 | m AGL |
| Aplicabilidad | VHF Radiodifusión | 5G/6G planificación | — |

---

## 2. Arquitectura de 5 Pasos

> **Nota operativa importante:** la implementación recibe distancias en metros y las convierte a kilómetros internamente. Como las tablas ITU son válidas a partir de 1 km, el código aplica `distances_km = max(distances_km, 1.0)` antes de interpolar. Por tanto, receptores a menos de 1 km del transmisor quedan extrapolados al mínimo válido del modelo.

### Paso 1: Calcular Altura Efectiva (h_eff)

$$h_{eff} = h_{tx} + z_{tx} - \overline{z}_{3-15km}$$

Donde:
- $h_{tx}$ = altura antena TX [m AGL]
- $z_{tx}$ = elevación terreno en TX [m MSL]
- $\overline{z}_{3-15km}$ = elevación media del terreno en anillo 3-15 km [m MSL]

**Implementación vectorizada (SIN FOR loops):**
```python
def _calculate_effective_height_vectorized(self, ...):
    # Si d < 15 km → P.1546-6 §4.3 usa h_tx_AGL
    short_path = distances < 15000.0

    # Con DEM y perfiles válidos: z_mean en rango 3–15 km
    h_eff_dem = h_tx + z_tx - z_mean_range

    h_eff = np.where(short_path, h_tx, h_eff_dem)
    h_eff = np.clip(h_eff, -3000.0, 1200.0)
    return h_eff
```

**Complejidad:** O(n × m) vectorizado, ~0.5ms para 10k receptores.

### Paso 2: Interpolar Intensidad de Campo (E [dBμV/m])

Tres tablas digitalizadas ITU (100, 600, 2000 MHz) con estructura:
```python
TABLE_100_MHZ = {
    1:    {10: 142, 20: 144, 37.5: 150, 75: 154, ...},  # distancia 1 km
    5:    {10: 125, 20: 128, 37.5: 137, 75: 145, ...},  # distancia 5 km
    ...
    1000: {10: 60, 20: 64, 37.5: 79, 75: 96, ...}       # distancia 1000 km
}
```

**Interpolación 3D vectorizada:**
```python
def get_reference_field_intensity(freq_mhz, distances_km, h_eff_m):
    # 1. Interpolar frecuencia (log-linear)
    if freq <= 600:
        E_100 = interpolate_distance_height(100, dist, h_eff, TABLE_100)
        E_600 = interpolate_distance_height(600, dist, h_eff, TABLE_600)
        ratio = (log10(freq) - log10(100)) / (log10(600) - log10(100))
        E = E_100 + ratio * (E_600 - E_100)
    else:
        E_600 = interpolate_distance_height(600, dist, h_eff, TABLE_600)
        E_2000 = interpolate_distance_height(2000, dist, h_eff, TABLE_2000)
        ratio = (log10(freq) - log10(600)) / (log10(2000) - log10(600))
        E = E_600 + ratio * (E_2000 - E_600)
    
    # 2. Interpolar distancia (log-linear dentro de tabla)
    # 3. Interpolar altura (linear dentro de tabla)
    return E  # Array [dBμV/m]
```

### Paso 3: Calcular Terrain Clearance Angle (TCA)

$$\theta_{TCA} = \max_i \left\{ \arctan \left( \frac{z_i - z_{TX} - h_{ant,TX}}{d_i} \right) \right\}$$

**Corrección TCA real implementada:**

Si el mayor ángulo de despeje respecto del receptor es no positivo, la corrección es 0 dB. Si existe obstáculo sobre la horizontal del receptor, se aplica directamente la función $J(\theta)$ de P.1546-6 §4.5, que introduce una penalización positiva en pérdida de trayecto.

**Implementación vectorizada:**
```python
def _calculate_tca_correction_vectorized(self, ...):
    theta_tc = np.maximum(theta_masked.max(axis=1), 0.0)
    t = theta_tc - 0.1
    J_theta = 6.9 + 20.0 * np.log10(np.sqrt(t * t + 1.0) + t)
    return np.where(theta_tc > 0.0, J_theta, 0.0)
```

### Paso 4: Aplicar Correcciones (Clutter + Percentiles)

La versión actual no usa una tabla fija por ambiente. La corrección de clutter se delega al `ClutterModel` y sigue el esquema de **ITU-R P.2108-1 §3**, dependiente de frecuencia, altura del receptor, distancia al clutter y, cuando existen perfiles, de la morfología inferida del terreno.

```python
clutter = ClutterModel(xp=self.xp)
clutter_correction = clutter.calculate_clutter_correction_vectorized(
    terrain_profiles=terrain_profiles,
    profile_distances=profile_distances,
    distances_m=distances_km * 1000.0,
    h_rx_agl=rx_height,
    environment=environment.lower(),
    frequency_mhz=frequency,
)
```

Además, el modelo aplica una corrección de percentil temporal y de ubicación conforme a **P.1546-6 §8.1**, sumando algebraicamente ambos ajustes. Solo existen tablas para `{1, 10, 50, 90, 99}`; percentiles intermedios se mapean al valor ITU más cercano disponible.

### Paso 5: Convertir E → Path Loss

$$PL_{[dB]} = 139.3 + 20 \log_{10}(f_{[MHz]}) - E_{[dB\mu V/m]} + \Delta E_{TCA} + \Delta E_{clutter} + \Delta E_{percentile}$$

Donde:
- 139.3 = constante normalizadora ITU (20·log₁₀(10⁷))
- $E$ = intensidad de campo desde tablas [dBμV/m]
- $\Delta E_{TCA}$ = corrección terrain clearance angle
- $\Delta E_{clutter}$ = corrección por clutter según ITU-R P.2108-1
- $\Delta E_{percentile}$ = corrección de tiempo y ubicación según P.1546-6 §8.1

```python
def _convert_field_to_path_loss(self, E_field, freq_mhz, tca_corr, clutter_corr, percentile_corr):
    path_loss = (139.3 
                 + 20 * np.log10(freq_mhz) 
                 - E_field 
                 + tca_corr 
                 + clutter_corr
                 + percentile_corr)
    return path_loss
```

---

## 3. Metadatos del Modelo (`get_model_info()`)

```python
{
    'name': 'ITU-R P.1546-6',
    'type': 'empirical_point_area',
    'frequency_range': '30-4000 MHz (tablas internas: 100-2000 MHz)',
    'distance_range': '1-1000 km',
    'tx_height_range': '10-1200 m AGL (tablas); extrapolación fuera de rango',
    'rx_height_range': '1-20 m AGL',
    'environments': ['Urban', 'Suburban', 'Rural'],
    'has_terrain_awareness': True,
    'has_tca_correction': True,      # P.1546-6 §4.5
    'has_clutter_correction': True,  # ITU-R P.2108-1 §3
    'cpu_gpu_support': True,
    'has_los_nlos': False,           # P.1546 es estadístico/punto-área; NO usa estados LOS/NLOS binarios
    'reference': 'ITU-R P.1546-6 (August 2019) + ITU-R P.2108-1',
    'implementation': 'Table interpolation (§4.3 h_eff, §4.5 TCA, §8.1 percentiles) + P.2108-1 clutter',
}
```

> **Nota clave:** `has_los_nlos = False` — el modelo es estadístico/punto-área; la variable
> de terreno se captura de forma continua a través del TCA (§4.5), no mediante un flag LOS/NLOS binario.

---

## 4. Corrección de Percentil (§8.1)

**Método:** `_apply_percentile_correction(percentile, freq_mhz, distances_km)`

P.1546-6 §8.1 define correcciones por percentil de tiempo en **suma algebraica**
(no cuadrática) aplicada como ajuste de campo `ΔE_percentile [dB]`.

Los únicos percentiles con tablas en el estándar ITU son: **{1, 10, 50, 90, 99}**.
Los percentiles intermedios se redondean al valor de tabla más cercano disponible:

```python
percentile_map = {1: 1, 5: 10, 10: 10, 25: 10, 50: 50, 75: 90, 90: 90, 95: 90, 99: 99}
```

La corrección se incorpora algebraicamente en la conversión E→PL:

$$PL = 139.3 + 20\log_{10}(f) - (E + \Delta E_{TCA} + \Delta E_{clutter} + \Delta E_{percentile})$$

---

## 5. Método Informativo: `_calculate_radio_horizon()`

**Fórmula estándar** (Radio Engineering, k = 4/3):

$$d_{ho} = 4.12\,(\sqrt{h_{TX}} + \sqrt{h_{RX}}) \quad [\text{km}]$$

```python
def _calculate_radio_horizon(self, tx_height: float, rx_height: float) -> float:
    return 4.12 * (math.sqrt(tx_height) + math.sqrt(rx_height))
```

> **Importante:** Este método **NO se invoca** dentro de `calculate_path_loss()`.
> P.1546-6 no usa el radio horizonte explícitamente en su procedimiento de cálculo
> de path loss. El método existe como utilidad informativa (referencia de cobertura
> teórica máxima LOS), pero el pipeline del modelo opera íntegramente con la
> interpolación de tablas y la corrección TCA continua.
